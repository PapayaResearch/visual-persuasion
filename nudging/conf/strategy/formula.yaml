_target_: nudging.formula.FormulaEdit

# Strategy name for logging/results organization
name: formula

# The learned formula prompt from auto-interpretation analysis
formula_prompt: |
  Make the item in the image more appealing to viewers.

# Use default image editor from config
image_editing_model: ${editor}
